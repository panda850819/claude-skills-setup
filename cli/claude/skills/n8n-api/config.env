# n8n API Configuration
#
# IMPORTANT: API Key 應從 ~/.zshrc 讀取，這裡不再維護
# 使用方式: source ~/.zshrc (不是 source 這個檔案)

N8N_API_URL="https://n8n.pdzeng.com"

# API Key 請從 ~/.zshrc 中的 N8N_API_KEY 環境變數讀取
# 原因: 這個檔案的 key 容易過期，而 ~/.zshrc 是主要來源

# 讀取範例:
# source ~/.zshrc 2>/dev/null
# curl -s "${N8N_API_URL}/api/v1/workflows" -H "X-N8N-API-KEY: ${N8N_API_KEY}"
